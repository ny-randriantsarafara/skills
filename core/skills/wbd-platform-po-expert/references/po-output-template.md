# WBD PO Output Template

Use this structure for product responses.

## 1. Objective

- Business goal:
- User problem:
- Success metric:
- Deadline / constraint:

## 2. Scope

- In scope:
- Out of scope:
- Assumptions:

## 3. Repository Impact Matrix

| Repository | Why impacted | Planned change | Contract impact |
| --- | --- | --- | --- |
| `<repo>` | `<reason>` | `<feature/work item>` | `<api/queue/env/schema>` |

## 4. Business Rules and Validation

- Multiplex business definition (context/rule):
- Highlight side effects today:
- Highlight side effects after change:
- Visibility rules:
- Mandatory rules:
- Validation enforcement:
- Editorial flow scope (`manual` / `ingestion` / `both`):
- Existing entry prerequisites/gates:
- Regression guardrails (must remain unchanged):

## 5. Delivery Slices

| Slice | Outcome | Repos | Dependencies | Exit criteria |
| --- | --- | --- | --- | --- |
| `S1` | `<value>` | `<repos>` | `<deps>` | `<testable criteria>` |
| `S2` | `<value>` | `<repos>` | `<deps>` | `<testable criteria>` |

## 6. Risk and Mitigation

| Risk | Severity | Mitigation | Owner |
| --- | --- | --- | --- |
| `<risk>` | `<H/M/L>` | `<action>` | `<team/repo>` |

## 7. Rollout Plan

- Rollout sequence:
- Feature flag / config strategy:
- Rollback trigger:
- Rollback action:

## 8. Open Questions

- `<question 1>`
- `<question 2>`

## 9. Evidence

- `<path/to/evidence-1>`
- `<path/to/evidence-2>`
